apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: home-dashboard-config
  namespace: prometheus
data:
  home.json: |-
{{ ( .Files.Get "dashboard/home.json") | indent 4 }}
