apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: github-dashboard-config
  namespace: prometheus
data:
  github.json: |-
{{ ( .Files.Get "dashboard/github.json") | indent 4 }}
