his project is a deployement of prometheus stack threw argocd on k3s local cluster

This project depends on the [k3s] and  [argocd] repositories, which contain deployement of a local K3s cluster on WSL